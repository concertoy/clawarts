# OpenAI auth - optional if logged in via Codex CLI (~/.codex/auth.json)
OPENAI_API_KEY=                 # API key (fallback if no Codex CLI token)

# Slack - both required
SLACK_BOT_TOKEN=      # Bot User OAuth Token
SLACK_APP_TOKEN=